<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="IndicatorsMapper">
	<!-- id는 마음대로 작성 prarameter는 실제DTO클래스 -->
	
	<!-- 모든 지표 리스트 보기 -->
	<select id="IndicatorsListAll" resultType="IndicatorsDTO">
		SELECT *
		FROM INDICATORS_TB
		ORDER BY INDICATORS_NUM
	</select>
	
	<!-- 카테고리 선택 지표 리스트 보기 -->
	<select id="IndicatorsListKategorie" parameterType="IndicatorsDTO" resultType="IndicatorsDTO">
		SELECT *
		FROM INDICATORS_TB
		WHERE 
			INDICATORS_NUM not like 0
			<if test="IS_BEST == 1">
				AND IS_BEST = #{IS_BEST}
			</if>
			<if test="IS_AGENCY == 1">
				AND IS_AGENCY = #{IS_AGENCY} 
			</if>
			<if test="IS_AHA == 1">
				AND IS_AHA = #{IS_AHA} 
			</if>
			<if test="IS_LINC == 1">
				AND IS_LINC = #{IS_LINC}
			</if>
			<if test="IS_TYPE3 == 1">
				AND IS_TYPE3 = #{IS_TYPE3}
			</if>
			<if test="!DIVISION_NAME.equals('모두 보기')">
				AND DIVISION_NAME = #{DIVISION_NAME}
			</if>
		ORDER BY INDICATORS_NUM
	</select>
	<!-- 특정 지표 보기 -->
	<select id="IndicatorsOne" parameterType="IndicatorsDTO" resultType="IndicatorsDTO">
		SELECT *
		FROM INDICATORS_TB
		WHERE INDICATORS_NUM = #{INDICATORS_NUM}
	</select>
</mapper>